<Window x:Class="AgentDock.DesktopHost.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AgentDock" 
        Height="900" Width="1400"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize"
        Background="#000000">
    
    <Window.Resources>
        <Style x:Key="WindowButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1A1A1A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E81123"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32"  
                      ResizeBorderThickness="{x:Static SystemParameters.WindowResizeBorderThickness}"
                      GlassFrameThickness="1"
                      CornerRadius="0"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Border x:Name="MainBorder" BorderBrush="#333333" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Border Grid.Row="0" Background="#000000" Height="32" BorderBrush="#333333" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- App Title / Logo Area -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Center">
                        <Path Data="M12 2L2 22h20L12 2z" Fill="#EDEDED" Width="14" Height="14" Stretch="Uniform"/>
                        <TextBlock Text="AgentDock" Foreground="#EDEDED" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" FontFamily="Segoe UI" Margin="10,0,0,0"/>
                        <Border Background="#333333" Width="1" Height="12" Margin="10,0"/>
                        <TextBlock x:Name="StatusText" Text="Ready" Foreground="#666666" FontSize="12" VerticalAlignment="Center" FontFamily="Segoe UI"/>
                    </StackPanel>

                    <!-- Window Controls -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button x:Name="MinimizeButton" Click="MinimizeButton_Click" Width="46" Height="32" Background="Transparent" BorderThickness="0" Style="{StaticResource WindowButtonStyle}" ToolTip="Minimizar">
                            <Path Data="M0,0 L10,0" Stroke="#999999" StrokeThickness="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="10"/>
                        </Button>
                        <Button x:Name="MaximizeButton" Click="MaximizeButton_Click" Width="46" Height="32" Background="Transparent" BorderThickness="0" Style="{StaticResource WindowButtonStyle}" ToolTip="Maximizar">
                            <Rectangle Width="10" Height="10" Stroke="#999999" StrokeThickness="1" Fill="Transparent"/>
                        </Button>
                        <Button x:Name="CloseButton" Click="CloseButton_Click" Width="46" Height="32" Background="Transparent" BorderThickness="0" Style="{StaticResource CloseButtonStyle}" ToolTip="Fechar">
                            <Path Data="M0,0 L10,10 M0,10 L10,0" Stroke="#999999" StrokeThickness="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="10" Height="10"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <ContentControl x:Name="WebViewContainer" Grid.Row="1" />
        </Grid>
    </Border>
</Window>
